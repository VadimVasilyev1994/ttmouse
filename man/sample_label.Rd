% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectors.R
\name{sample_label}
\alias{sample_label}
\title{Build a metadata label for a sample (for plot annotation)}
\usage{
sample_label(object, sample_num, train_or_test = c("test", "train"))
}
\arguments{
\item{object}{A post-projection object from \code{\link[=project_test_data]{project_test_data()}}.}

\item{sample_num}{Sample index (1-based).}

\item{train_or_test}{Which metadata block to read; \code{"test"} (default) or
\code{"train"}.}
}
\value{
A single string like \code{"group_1: Adrenal | group_2: ALF | time: 0"},
or \code{""} if nothing is available.
}
\description{
Returns a short, human-readable string of the metadata stored for one sample
-- whichever of \code{group_1/2/3}, \code{time}, \code{replicate} are present (non-NA) -- for
annotating the per-sample plots. Returns \code{""} when no metadata is available
(e.g. the intergene path with no metadata file), so callers can skip the
annotation.
}
\details{
Verified against VadimVasilyev1994/TimeTeller-v2 (commit f3cbd44),
\code{add_test_data()}: the supplied metadata is stored under
\code{object$Metadata$Test$Group_1}/\code{Group_2}/\code{Group_3}/\code{Time}/\code{Replicate}, each a
vector of length n_samples (NA-filled when not supplied).
}
