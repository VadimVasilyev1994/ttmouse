% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_helpers.R
\name{metadata_to_projection_args}
\alias{metadata_to_projection_args}
\title{Map a metadata table to project_test_data() arguments}
\usage{
metadata_to_projection_args(metadata, require_groups = TRUE)
}
\arguments{
\item{metadata}{A data.frame from \code{\link[=read_metadata]{read_metadata()}} (canonical columns).}

\item{require_groups}{If \code{TRUE} (default, time-course path), at least one
grouping column (\code{group_1/2/3} or \code{replicate}) must be present. Set \code{FALSE}
for the intergene path, where nothing is required.}
}
\value{
A named list of \code{\link[=project_test_data]{project_test_data()}} arguments (any of \code{test_time},
\code{test_group_1/2/3}, \code{test_replicate}); possibly empty when nothing is
present and \code{require_groups = FALSE}.
}
\description{
Converts a metadata data.frame (as returned by \code{\link[=read_metadata]{read_metadata()}}, with
canonical column names) into the named argument list for \code{\link[=project_test_data]{project_test_data()}}:
whichever of \code{time}, \code{group_1/2/3}, \code{replicate} are present, mapped to the
corresponding \verb{test_*} arguments.
}
\details{
Verified against VadimVasilyev1994/TimeTeller-v2 (commit f3cbd44): the plain
\code{timecourse} normalisation derives its grouping from \code{test_group_1/2/3} +
\code{test_replicate} and errors ("No groups specified") if none are supplied, so
on that path at least one grouping column is required (\code{require_groups = TRUE}). The intergene normalisation ignores grouping/time entirely (it
z-scores per sample), so there nothing is required (\code{require_groups = FALSE});
any supplied columns are still stored and surface in \code{Results_df}. Neither
path uses \code{test_grouping_vars} (matched/combined only), so it is not produced.
}
