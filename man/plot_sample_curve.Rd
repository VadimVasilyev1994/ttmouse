% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_sample_curve}
\alias{plot_sample_curve}
\title{Plot a sample's theta-calculation curves}
\usage{
plot_sample_curve(
  object,
  sample_num,
  logthresh = model_log_thresh(object),
  train_or_test = c("test", "train")
)
}
\arguments{
\item{object}{A post-projection object (for \code{"test"}) or a model (for
\code{"train"}).}

\item{sample_num}{Sample index; one of \code{1:n_samples(object, train_or_test)}.}

\item{logthresh}{Log threshold; defaults to \code{model_log_thresh(object)}.}

\item{train_or_test}{Plot the \code{"test"} (default) or \code{"train"} sample.}
}
\value{
A recorded plot (\code{recordPlot()}), as returned by the underlying
function.
}
\description{
Wraps TimeTeller's \code{plot_ind_curve()}: the per-sample theta / flat-likelihood
diagnostic. Base graphics; returns the plot as a \code{recordPlot()} object (as the
underlying function does). \code{logthresh} defaults to the model's stored
threshold via \code{\link[=model_log_thresh]{model_log_thresh()}}.
}
