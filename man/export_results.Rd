% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_results.R
\name{export_results}
\alias{export_results}
\title{Export projection results to a CSV (prediction-relevant subset)}
\usage{
export_results(object, path = NULL, columns = RESULT_EXPORT_COLS)
}
\arguments{
\item{object}{A post-projection object from \code{\link[=project_test_data]{project_test_data()}}.}

\item{path}{Output CSV path. If \code{NULL} (default), no file is written and the
data.frame is returned for inspection or display.}

\item{columns}{Character vector of \code{Results_df} columns to keep, in order;
defaults to the prediction-relevant subset.}
}
\value{
Invisibly, the exported data.frame (\code{Sample} plus the present
columns).
}
\description{
Builds the per-sample prediction subset of \code{Test_Data$Results_df}: a leading
\code{Sample} column (the \code{Results_df} rownames, i.e. the sample names) followed by
whichever of \code{columns} are present. The 16 internal peak-diagnostic columns
are dropped. Columns that are absent on a given path (for example the
\verb{Group_*}/\code{Actual_Time}/\code{Pred_Error} columns on the single-sample intergene
path, where no metadata was supplied) are quietly skipped rather than causing
an error, so the same call works for both projection paths.
}
