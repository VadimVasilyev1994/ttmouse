% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_sample_likelihoods}
\alias{plot_sample_likelihoods}
\title{Plot a sample's raw truncated likelihood curves}
\usage{
plot_sample_likelihoods(
  object,
  sample_num,
  logthresh = model_log_thresh(object),
  train_or_test = c("test", "train")
)
}
\arguments{
\item{object}{A post-projection object (for \code{"test"}) or a model (for
\code{"train"}).}

\item{sample_num}{Sample index; one of \code{1:n_samples(object, train_or_test)}.}

\item{logthresh}{Log threshold for truncation; defaults to
\code{model_log_thresh(object)}.}

\item{train_or_test}{Plot the \code{"test"} (default) or \code{"train"} sample.}
}
\value{
Invisibly \code{NULL} (called for its plot side effect).
}
\description{
Wraps TimeTeller's \code{plot_raw_likelis()}. Draws to the active graphics device
(base graphics) and returns nothing useful. \code{logthresh} defaults to the
model's stored training threshold via \code{\link[=model_log_thresh]{model_log_thresh()}}.
}
