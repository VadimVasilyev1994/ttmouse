% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectors.R
\name{n_samples}
\alias{n_samples}
\title{Number of samples available to the per-sample plots}
\usage{
n_samples(object, train_or_test = c("test", "train"))
}
\arguments{
\item{object}{A post-projection object (for \code{"test"}) or a model (for
\code{"train"}).}

\item{train_or_test}{Which likelihood array to size; \code{"test"} (default) or
\code{"train"}.}
}
\value{
Integer count of samples (valid \code{sample_num} is \code{1:n}).
}
\description{
Returns how many samples the per-sample diagnostic plots
(\code{\link[=plot_sample_likelihoods]{plot_sample_likelihoods()}}, \code{\link[=plot_sample_curve]{plot_sample_curve()}}) can index, for
populating a sample selector. The valid \code{sample_num} values are \code{1:n}.
}
\details{
Verified against VadimVasilyev1994/TimeTeller-v2 (commit f3cbd44):
\code{plot_raw_likelis()} indexes \code{likelis_array[, sample_num, ]}, where the array
is \code{Test_Data$Test_Likelihood_Array} (test) or
\code{Train_Data$Train_Likelihood_Array} (train); the sample axis is the 2nd
dimension.
}
