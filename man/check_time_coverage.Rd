% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_time_coverage.R
\name{check_time_coverage}
\alias{check_time_coverage}
\title{Check per-group time coverage before time-course projection}
\usage{
check_time_coverage(
  test_time,
  test_group_1 = NULL,
  test_group_2 = NULL,
  test_group_3 = NULL,
  test_replicate = NULL,
  min_span_hours = 12
)
}
\arguments{
\item{test_time}{Numeric sample times, in hours (one per sample). Character
values that parse as numbers are accepted; anything else is an error.}

\item{test_group_1, test_group_2, test_group_3, test_replicate}{Optional grouping
vectors (one value per sample), as produced by
\code{\link[=metadata_to_projection_args]{metadata_to_projection_args()}}. Whichever are supplied define the groups;
at least one must be supplied (the time-course path always supplies one).}

\item{min_span_hours}{Smallest acceptable per-group span. A group is flagged
when its span is \strong{<=} this value (default 12); a full ~24 h circadian
cycle is recommended for reliable results.}
}
\value{
A list with: \code{ok} (TRUE only if every group's span exceeds
\code{min_span_hours}), \code{n_groups}, \code{n_failing}, a \code{failing} data.frame
(\code{group}, \code{n}, \code{span}) for the flagged groups (smallest span first), the
overall smallest \code{min_span}, and a display \code{summary} string.
}
\description{
Time-course normalisation scales each time-series group across its own
samples (TimeTeller-v2 \code{normalise_test_data()}, the plain \code{timecourse}
method). When a group covers only a small window of circadian time, that
within-group scaling is unreliable. This check computes each group's time
span (\code{max(time) - min(time)}, in hours) and flags any whose span does not
exceed \code{min_span_hours}.
}
\details{
Groups are formed exactly as the model forms them: by concatenating
\code{group_1/2/3} and \code{replicate} with \code{NA} treated as \code{""}
(TimeTeller-v2 @ f3cbd44, \code{Testing_functions.R}:
\code{paste0(replace_na(g1,''), ..., replace_na(rep,''))}). The group key is used
verbatim as the group label, matching TimeTeller's own group naming.

Like \code{\link[=validate_genes]{validate_genes()}}, this reports failures (it does not stop on them) so
the app can show a clear message and block projection gracefully; it only
stops on malformed input (non-numeric times, or grouping vectors whose length
does not match \code{test_time}).
}
