% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selectors.R
\name{local_projection_choices}
\alias{local_projection_choices}
\title{Available local-projection choices for a model}
\usage{
local_projection_choices(model)
}
\arguments{
\item{model}{A model loaded by \code{\link[=load_model]{load_model()}} (or a post-projection object;
both carry \code{Projections$All_Projections}).}
}
\value{
A character vector of local-projection labels (the \code{Time_} prefix
stripped), each usable as a \code{selected_local_projection} value.
}
\description{
Lists the local projections a model offers, for populating a selector (e.g. a
Shiny dropdown) of valid \code{selected_local_projection} values.
}
\details{
Verified against VadimVasilyev1994/TimeTeller-v2 (commit f3cbd44): the plot
functions build \code{paste0('Time_', selected_local_projection)} and index
\code{object$Projections$All_Projections} with it. Each local projection is stored
there under a \verb{Time_<label>} name, so the valid selector values are those
names with the \code{Time_} prefix removed.
}
