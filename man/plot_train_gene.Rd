% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_train_gene.R
\name{plot_train_gene}
\alias{plot_train_gene}
\title{Plot a training gene's expression over time, grouped, with replicate SE bars}
\usage{
plot_train_gene(
  model,
  gene,
  group_var = "Group",
  gene_label = NULL,
  interactive = FALSE
)
}
\arguments{
\item{model}{A model from \code{\link[=load_model]{load_model()}}.}

\item{gene}{Ensembl ID of the gene to plot (must be among the training genes).}

\item{group_var}{Column in \code{Train$Data} to group by: \code{"Group"} (group_1,
default), \code{"Group_2"}, or \code{"Group_3"}.}

\item{gene_label}{Optional display label for the title (e.g. a gene symbol);
defaults to \code{gene}.}

\item{interactive}{If \code{TRUE}, wrap the plot with \code{plotly::ggplotly()} and
return an interactive plotly widget whose legend toggles groups on click
(double-click to isolate one). Defaults to \code{FALSE} (a static ggplot).}
}
\value{
A ggplot object, or a plotly object when \code{interactive = TRUE}.
}
\description{
Plots one gene's expression across training time, with one series per level of
the chosen grouping variable and error bars showing the standard error across
replicates at each time point. Uses the tidy training data in \code{Train$Data} and
the numeric times in \code{Metadata$Train$Time}.
}
