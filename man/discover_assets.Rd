% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_helpers.R
\name{discover_assets}
\alias{discover_assets}
\title{Discover the runtime data assets (organ models + shared gene lengths)}
\usage{
discover_assets(data_dir = NULL, gene_lengths = NULL)
}
\arguments{
\item{data_dir}{Directory containing \code{gene_lengths.rds} and the organ
subdirectories; defaults to the \code{TTMOUSE_DATA_DIR} environment variable.}

\item{gene_lengths}{Optional explicit path to the gene-length table,
overriding the conventional top-level \code{gene_lengths.rds}.}
}
\value{
A list with \code{gene_lengths} (a verified file path) and \code{organs} (a
named list; each organ is itself a named list of the model file paths that
exist, among \code{intergene} and \code{timecourse}).
}
\description{
Resolves the data directory (the \code{data_dir} argument, else the
\code{TTMOUSE_DATA_DIR} environment variable) and discovers the assets within it: a
shared \code{gene_lengths.rds} at the top level, and one subdirectory per organ
containing \code{intergene.rds} and/or \code{timecourse.rds}. An organ is included if it
has at least one of the two model files; an organ may provide only one type. A
single organ is fine (no error).
}
\details{
Expected layout:
\preformatted{
data_dir/
  gene_lengths.rds        # shared, organ-independent
  liver/  intergene.rds  timecourse.rds
  scn/    intergene.rds  timecourse.rds
}
}
