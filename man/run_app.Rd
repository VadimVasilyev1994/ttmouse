% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Launch the ttmouse Shiny application}
\usage{
run_app(
  data_dir = NULL,
  gene_lengths = NULL,
  host = "0.0.0.0",
  port = NULL,
  launch.browser = interactive(),
  max_upload_mb = 50
)
}
\arguments{
\item{data_dir}{Directory with \code{gene_lengths.rds} and the organ
subdirectories; defaults to the \code{TTMOUSE_DATA_DIR} environment variable.}

\item{gene_lengths}{Optional explicit path to the gene-length table,
overriding the conventional top-level \code{gene_lengths.rds}.}

\item{host}{Host passed to \code{\link[shiny:runApp]{shiny::runApp()}}; defaults to \code{"0.0.0.0"} so the
app is reachable when containerised.}

\item{port}{Port to listen on. When \code{NULL} (the default), the \code{PORT}
environment variable is used if set (e.g. Cloud Run sets it); otherwise the
port falls back to 7860 (the Hugging Face Spaces convention). This lets a
single container serve on either platform unchanged. Pass a value to
override.}

\item{launch.browser}{Passed to \code{\link[shiny:runApp]{shiny::runApp()}}; defaults to \code{TRUE}
interactively.}

\item{max_upload_mb}{Maximum size, in megabytes, of an uploaded file; sets the
\code{shiny.maxRequestSize} option (Shiny's own default is only 5 MB). Defaults
to 50, comfortably covering typical count matrices; raise it for very large
studies.}
}
\value{
Invisibly \code{NULL}; the function runs the app until it is stopped.
}
\description{
Discovers and preloads the runtime assets once at startup -- the shared
gene-length table and every organ's models -- then launches the bundled Shiny
app. Preloaded assets are passed to the app via the \code{ttmouse.assets} option
and shared across sessions, so they are read from disk only once.
}
\details{
Assets are located by \code{\link[=discover_assets]{discover_assets()}}: pass \code{data_dir} (containing
\code{gene_lengths.rds} and one subdirectory per organ, each with \code{intergene.rds}
and/or \code{timecourse.rds}) or set the \code{TTMOUSE_DATA_DIR} environment variable. A
single organ is fine. A missing or invalid asset fails loudly here, at
startup, rather than mid-session.
}
