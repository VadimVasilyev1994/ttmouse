% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_training_projection}
\alias{plot_training_projection}
\title{Plot the training projection in 3D}
\usage{
plot_training_projection(
  model,
  selected_local_projection,
  density = FALSE,
  ...
)
}
\arguments{
\item{model}{A model loaded by \code{\link[=load_model]{load_model()}}.}

\item{selected_local_projection}{One of \code{\link[=local_projection_choices]{local_projection_choices()}}.}

\item{density}{Draw covariance ellipsoids? Kept \code{FALSE} by default so the
plot stays pure-plotly (no \code{rgl}/OpenGL), which is headless-Docker-safe.}

\item{...}{Forwarded to \code{plot_3d_projection()} (e.g. \code{opacity}, \code{sig_level},
used only when \code{density = TRUE}).}
}
\value{
A plotly object.
}
\description{
Wraps TimeTeller's \code{plot_3d_projection()}: the model's own training data
projected onto a chosen local projection. Returns a plotly object.
}
