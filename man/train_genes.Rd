% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_train_gene.R
\name{train_genes}
\alias{train_genes}
\title{List the training genes (for a gene selector), labelled by symbol}
\usage{
train_genes(model, gene_lengths = NULL)
}
\arguments{
\item{model}{A model from \code{\link[=load_model]{load_model()}}.}

\item{gene_lengths}{Optional gene-length table (with \code{gene_id}, \code{gene_name}
columns) used to map Ensembl IDs to symbols. When \code{NULL}, Ensembl IDs are
used as the labels.}
}
\value{
Named character vector: names = display labels, values = Ensembl IDs.
}
\description{
Returns the genes used by the model as a named character vector mapping a
display label (gene symbol where available, otherwise the Ensembl ID) to the
Ensembl ID -- suitable for a Shiny \code{selectInput} where the user picks by
symbol but the value passed on is the Ensembl ID.
}
