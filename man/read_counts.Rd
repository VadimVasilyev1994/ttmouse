% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_counts.R
\name{read_counts}
\alias{read_counts}
\title{Read an uploaded raw-count file into a numeric matrix}
\usage{
read_counts(path, sheet = 1, verbose = FALSE)
}
\arguments{
\item{path}{Path to the uploaded \code{.csv} or \code{.xlsx} file.}

\item{sheet}{For \code{.xlsx}, the worksheet to read (name or index; default first).}

\item{verbose}{If \code{TRUE}, print a one-line summary.}
}
\value{
A numeric matrix (genes x samples) with gene IDs as rownames.
}
\description{
Reads an uploaded raw-count file (CSV or XLSX) and returns the clean numeric
matrix the rest of the pipeline expects: genes in rows, samples in columns,
rownames = gene IDs. This is the ingestion front-door before
\code{\link[=preprocess_counts]{preprocess_counts()}}.
}
\details{
Input file contract: column 1 holds the gene IDs (used as rownames) and every
other column is one numeric sample-count column (no annotation columns). CSV is
comma-separated; XLSX is read from \code{sheet} (default the first) with the first
row as the header.

Only the \emph{structure} is validated here (a numeric matrix with unique gene-ID
rownames and named sample columns); count \emph{values} (finite, non-negative) are
checked later in \code{\link[=preprocess_counts]{preprocess_counts()}}, so they are not duplicated.
}
