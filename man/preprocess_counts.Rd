% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_counts.R
\name{preprocess_counts}
\alias{preprocess_counts}
\title{Convert raw counts to the model's training scale (log2 TPM + 1)}
\usage{
preprocess_counts(counts, gene_lengths, verbose = FALSE)
}
\arguments{
\item{counts}{Raw count matrix (genes x samples) with gene IDs as rownames.}

\item{gene_lengths}{Data frame with columns \code{gene_id}, \code{gene_length},
\code{gene_name} (built once by \code{data-raw/build_gene_lengths.R}).}

\item{verbose}{If \code{TRUE}, print progress.}
}
\value{
A numeric matrix of log2(TPM + 1) values with Ensembl rownames.
}
\description{
Thin wrapper that converts a raw count matrix into the exact expression scale
the pre-built mouse models were trained on: log2(TPM + 1) with Ensembl gene
IDs. Its only jobs are to (1) pin the frozen preprocessing contract so it can
never drift, and (2) guard that the input really is a raw count matrix. All
gene-ID handling and gene-length matching is delegated to \code{counts_to_tpm()}.
}
\details{
The frozen contract (must match how the models were trained) is
\code{return_format = "ensembl"}, \code{log_transform = TRUE}, \code{prior_count = 1}, and
\code{skip_filter = TRUE} (projection mode never drops genes, or a model's required
genes could go missing and \code{test_model()} would hard-error).
}
\section{Dependency}{

Requires \code{counts_to_tpm()} to be available in the package (the user's own
function lives at \code{R/counts_to_tpm.R}). It uses \code{edgeR} and \code{DGEobj.utils}.
}

