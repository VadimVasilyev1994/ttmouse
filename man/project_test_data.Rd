% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{project_test_data}
\alias{project_test_data}
\title{Project a preprocessed matrix onto a pre-built TimeTeller model}
\usage{
project_test_data(
  model,
  expr_matrix,
  test_group_1 = NULL,
  test_group_2 = NULL,
  test_group_3 = NULL,
  test_replicate = NULL,
  test_time = NULL,
  test_grouping_vars = NULL,
  log_thresh = model_log_thresh(model),
  verbose = FALSE
)
}
\arguments{
\item{model}{A model loaded by \code{\link[=load_model]{load_model()}}.}

\item{expr_matrix}{Preprocessed matrix from \code{\link[=preprocess_counts]{preprocess_counts()}}.}

\item{test_group_1, test_group_2, test_group_3, test_replicate}{Optional grouping
vectors (required for the timecourse family; omit for intergene/clr).}

\item{test_time}{Optional known sample times (for validation).}

\item{test_grouping_vars}{Optional; used by the timecourse_matched/combined
methods.}

\item{log_thresh}{Log threshold; defaults to \code{model_log_thresh(model)}.}

\item{verbose}{If \code{TRUE}, print a one-line summary.}
}
\value{
The updated TimeTeller object (results in \code{Test_Data$Results_df}).
}
\description{
Runs the model on a user's preprocessed matrix and returns the updated
TimeTeller object (predictions in \code{object$Test_Data$Results_df}, e.g.
\code{time_1st_peak}, \code{time_2nd_peak}, \code{Theta}).
}
\details{
Verified behaviour (against VadimVasilyev1994/TimeTeller-v2, master):
\code{mat_normalised_test} is forced \code{TRUE} because the matrix is already on the
model scale (log2(TPM+1)); \code{log_thresh} defaults to the model's stored
training threshold so it is applied automatically; and unsupplied optional
arguments are \emph{omitted} (not passed as \code{NULL}) because \code{add_test_data()} uses
\code{missing()} to default them to \code{NA}. \code{intergene}/\code{clr} models need no groups;
the timecourse family requires grouping information.
}
